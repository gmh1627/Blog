# LEAP：面向 LLM 概率预测的似然 elicitation 与聚合

论文：Yufei Chen et al., *LEAP: Likelihood Elicitation and Aggregation for LLM-based Probabilistic Forecasting*  
版本：arXiv:2609.01337v1，2026-09-01  
链接：[论文摘要](https://arxiv.org/abs/2609.01337) ｜ [论文全文](https://arxiv.org/html/2609.01337v1) ｜ [代码仓库](https://github.com/layingfish/LEAP)

## 1. 核心观点

LEAP 不改变上游的搜索流程，重点改造“证据已经搜集完成之后，如何形成最终预测”这一阶段。

传统的单体式预测（Monolithic Prediction）把所有证据放入一个长上下文，让 LLM 一次性输出答案。LEAP 将其拆为两步：

```text
证据集合 E
  -> LLM 逐条解释证据，输出局部结构化判断
  -> 概率模型将这些判断与先验结合
  -> 输出后验分布、预测区间和证据级贡献
```

核心原则是：**LLM 负责局部证据解释，确定性概率模型负责最终计算。**

## 2. 两阶段系统

### 2.1 证据搜集阶段

真实 Agent 仍然需要先通过搜索、浏览和工具调用搜集信息。论文自己的系统使用 ReAct 式循环；外部实验则直接读取 DeerFlow、Hermes、OpenClaw 和 MiroFlow 等框架产生的 trace。

搜集结果被整理为带有文本、时间戳和来源依赖键的证据项：

\[
\mathcal{E}=\{e_1,e_2,\ldots,e_n\}
\]

### 2.2 预测聚合阶段

LEAP 在预测阶段不再让 LLM 读取整个证据集合。对于每个证据项 (e_i)，LLM 只看到问题、候选选项和该证据项本身，不能看到其他证据或当前后验结果。

论文规定使用严格的 JSON schema。LLM 输出的主要是定性标签，例如支持/反对/无关、强度、可靠性、目标匹配程度、来源层级和 dependency key；连续任务还需要提取证据暗示的数值位置。

这些字段不是最终概率。程序随后将它们转换为似然参数，并执行确定性的贝叶斯更新。

## 3. 似然参数如何产生

### 3.1 离散任务

对于单选任务，LLM 输出每个选项的局部支持判断。程序根据固定映射表将标签转换为非归一化似然 (L_i(k)=P(e_i\mid\theta=k))。

论文给出的典型锚点包括：

| 局部判断 | 似然映射的含义 |
| --- | --- |
| 无关证据 | 各选项接近 0.5，似然比接近 1，不改变先验 |
| 强支持选项 (k) | 支持选项约为 0.70–0.95，其他选项约为 0.05–0.20 |
| 强反对选项 (k) | 方向相反 |
| 直接验证结果 | 可以使用更强的 0.99/0.01，但仍受上限限制 |

例如，若程序采用 (L_i(A)=0.85,L_i(B)=0.15)，其含义是该证据对 A 相对于 B 的似然比为：

\[
\frac{L_i(A)}{L_i(B)}=5.67
\]

它不是 A 的最终概率。最终概率还取决于先验和其他证据。

需要特别说明：论文公开了范围、典型锚点以及可靠性和证据角色的调整规则，但没有在正文中完整列出区间内部每一种标签组合对应的具体数值分支。因此，不能简单理解为“LLM 在 0.70–0.95 中自由选择一个数字”。论文的设计意图是由固定程序映射完成；公开材料对完整实现细节的披露并不充分。

### 3.2 连续任务

连续目标采用高斯先验与高斯似然：

\[
\theta\sim\mathcal{N}(\mu_0,\sigma_0^2),\qquad
L_i(\theta)\propto\mathcal{N}(\theta;\mu_i,\sigma_i^2)
\]

- (mu_i)：LLM 从证据中提取或推断的目标值；
- (sigma_i)：主要由程序依据证据类型、来源等级、时效性、目标匹配程度和可靠性映射得到，而不是由 LLM 任意给出；
- 直接目标证据的不确定性较小，代理信号和背景信息的不确定性较大。

论文给出的连续任务相对标准差初值示例为：直接目标 0.08、代理信号 0.18、背景/上下文 0.45，并根据元数据进行乘法调整，最终限制在 0.03–1.25 之间。

## 4. 贝叶斯聚合

### 4.1 单选任务

带有先验强度和证据权重的后验为：

\[
\log\pi_{\mathrm{post}}(k)
\propto
\alpha\log\pi_0(k)
+\eta\sum_{i\in\mathcal{R}}w_i\log L_i(k)
\]

随后对所有选项做 softmax 归一化。

- (pi_0(k))：先验概率；
- (L_i(k))：第 (i) 条证据对选项 (k) 的似然；
- (w_i)：证据角色权重，论文给出的范围约为 0.05–1.5；
- (eta)：全局似然强度，主实验默认 1.0；
- (alpha)：先验强度。

### 4.2 多选任务

每个选项独立建模，使用 log-likelihood ratio：

\[
\ell_i(k)=\log\frac{p^{\mathrm{true}}_{i,k}}{p^{\mathrm{false}}_{i,k}}
\]

后验 log-odds 为：

\[
\operatorname{logit}(\pi_{\mathrm{post}}(k))
=
\operatorname{logit}(\pi_0(k))
+\eta\sum_iw_i\ell_i(k)
\]

各选项的概率不要求加总为 1。

### 4.3 连续任务

令精度 (	$\tau_x=1/\sigma_x^2$)，则：

\[
\tau_{\mathrm{post}}=\tau_0+\eta\sum_i\tau_i
\]

\[
\mu_{\mathrm{post}}
=
\frac{\mu_0\tau_0+\eta\sum_i\mu_i\tau_i}
{\tau_{\mathrm{post}}}
\]

最终从后验高斯分布读取中位数和预测分位数。

这是真实的数值计算，不是再次调用 LLM 生成“最终解释”。由于采用共轭分布，更新可以闭式完成。

## 5. 工程保护与可审计性

- **依赖聚类**：多个网页转载同一份报告时只保留一个代表，减少重复计数。
- **可靠性采样**：重复询问同一证据；若局部判断不一致，则缩小其似然强度。
- **异常值剔除**：连续任务中，若证据数值偏离数据先验均值超过 4 个标准差，则将其视为可能的单位或语义错误并排除。
- **Leave-one-out 贡献**：删除第 (j) 条证据并重新计算后验，报告预测变化 (Delta_j)。因此可以追踪每条证据对最终结果的实际影响。

## 6. 数值案例

论文用 AAPL 日最高价预测展示了完整审计链：

- 近期七个日高构造先验 (\mathcal{N}(250.74,7.5222^2))；
- 不同证据分别产生数值位置和不确定性；
- 一条 190 美元的分析师目标价距离先验超过 4 个标准差，被剔除；
- 最终后验为 (\mathcal{N}(250.157,5.7031^2))；
- 某条 Nasdaq 证据使后验均值下降约 0.67 美元。

## 7. 实验结果

论文构造了 347 个任务，来自 FutureX、GAIA 和 BrowseComp，包含单选、多选和连续预测。Monolithic 与 LEAP 对每个任务使用同一证据集合，并进行时间隔离，以便把差异归因于预测阶段。

主要结论：

- 在 5 个基础模型上，LEAP 均提升 FutureX 综合分、准确率和 Spherical score；连续任务的 NCRPS 均改善。
- 在 4 个外部 Agent 框架上，平均 FutureX 从 0.4491 提升到 0.5471，Brier score 从 0.4806 降到 0.3157。
- 诊断子集上的 ECE 从 0.1840 降到 0.0876，过度自信指标从 0.3167 降到 0.1500。
- 预测跨度越长，LEAP 的优势越明显，因为它倾向于扩大不确定性，而不是自信地押注错误选项。
- 代价是更多调用：诊断实验中每任务 token 数约从 5,733 增加到 11,508，p95 延迟从 12.1 秒增加到 27.8 秒。

需要注意，Brier score 并非所有模型都改善；论文的主要优势更稳定地体现在校准、概率质量和审计性上。
