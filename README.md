# Blog

铁路地图工程位于 `Railway` 子模块中。首次克隆并同时取得铁路工程：

```powershell
git clone --recurse-submodules https://github.com/gmh1627/Blog.git
```

已有 Blog 仓库可执行 `git submodule update --init --recursive`。铁路交互图入口为 `Railway/web/index.html`，本地运行方式见 `Railway/web/README.md`。
