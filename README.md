# Blog

地图工程位于 `Map` 子模块中。首次克隆并同时取得地图工程：

```powershell
git clone --recurse-submodules https://github.com/gmh1627/Blog.git
```

已有 Blog 仓库可执行 `git submodule update --init --recursive`。铁路交互图入口为 `Map/web/index.html`，本地运行方式见 `Map/web/README.md`。
